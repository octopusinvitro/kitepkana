---
engines:
  bundler-audit:
    enabled: false
  duplication:
    enabled: true
    config:
      languages:
      - ruby
  fixme:
    enabled: true
  rubocop:
    enabled: false
    channel: rubocop-0-52
ratings:
  paths:
  - Gemfile.lock
  - "**.js"
  - "**.rb"
exclude_paths:
- spec/
